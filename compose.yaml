services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    profiles: ["dev"]
    build:
      context: ./backend
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      ENVIRONMENT: dev
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      DJANGO_DEBUG: "True"
      DJANGO_SETTINGS_MODULE: config.settings
      ALLOWED_HOSTS: "backend,localhost,127.0.0.1"
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app

  backend_prod:
    profiles: ["prod"]
    build:
      context: ./backend
    command: gunicorn glazedin.wsgi:application --bind 0.0.0.0:8000 --workers 3
    environment:
      ENVIRONMENT: production
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      DJANGO_DEBUG: "False"
      DJANGO_SETTINGS_MODULE: config.settings
      ALLOWED_HOSTS: "backend_prod,localhost,127.0.0.1"
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"

  frontend:
    profiles: ["dev"]
    build:
      context: ./frontend
      args:
        BACKEND_URL: http://backend:8000
    environment:
      NODE_ENV: development
      PORT: 3000
    depends_on:
      - backend
    ports:
      - "3000:3000"

  frontend_prod:
    profiles: ["prod"]
    build:
      context: ./frontend
      args:
        BACKEND_URL: http://backend_prod:8000
    environment:
      NODE_ENV: production
      PORT: 3000
    depends_on:
      - backend_prod
    ports:
      - "3000:3000"

volumes:
  postgres_data: