apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: glazedin-staging
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Gorbiel/Software_Engineering_Web_Project.git
    targetRevision: dev
    path: k8s/overlays/staging

  destination:
    server: https://kubernetes.default.svc
    namespace: staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
